from __gin__ import dynamic_registration

from rave import blocks

blocks.GRU:
    num_layers = 1

blocks.Generator:
    recurrent_layer = @blocks.GRU

blocks.Encoder:
    recurrent_layer = @blocks.GRU